# ⚡ Transformers & Attention

- Attention mechanism (Query, Key, Value)
- Multi-head attention (parallel heads, projection)
- Positional encoding (sinusoidal encoding)
- Transformer architecture (encoder-decoder, LayerNorm, FFN)
- BERT & GPT (encoder-only vs decoder-only)
- Vision Transformer ViT (patches as tokens)
