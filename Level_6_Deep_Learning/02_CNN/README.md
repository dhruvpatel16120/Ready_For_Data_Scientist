# 🖼️ CNN — Convolutional Neural Networks

- Convolution operation (filters, feature maps, stride)
- Pooling layers (max, average, global average)
- Classic architectures (LeNet → AlexNet → VGG → ResNet → EfficientNet)
- Transfer learning (pre-trained weights, fine-tuning) — CRITICAL
- Object detection (YOLO, SSD, Faster R-CNN)
- Image segmentation (FCN, U-Net)
