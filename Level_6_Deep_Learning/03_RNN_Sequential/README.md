# 📝 RNN & Sequential Models

- Vanilla RNN (hidden state, BPTT, vanishing gradients)
- LSTM (cell state, forget/input/output gates)
- GRU (simplified LSTM, reset & update gates)
- Bidirectional RNNs (context from both directions)
- Seq2Seq (encoder-decoder, attention genesis)
