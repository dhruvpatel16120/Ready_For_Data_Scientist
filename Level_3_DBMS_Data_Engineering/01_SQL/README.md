# 🗃️ SQL — Basic to Advanced

- DDL commands (CREATE, ALTER, DROP, TRUNCATE)
- DML commands (SELECT, INSERT, UPDATE, DELETE)
- Filtering & sorting (WHERE, BETWEEN, IN, LIKE, ORDER BY)
- Aggregations (GROUP BY, HAVING, COUNT, SUM, AVG)
- JOINs (INNER, LEFT, RIGHT, FULL OUTER, CROSS, SELF)
- Subqueries (correlated vs non-correlated)
- Window functions (ROW_NUMBER, RANK, LEAD, LAG) — CRITICAL
- CTEs (WITH clause, recursive CTEs)
- Indexes (B-tree, composite, unique)
- Views & stored procedures
- Transactions & ACID
- SQL for data analysis (date/string functions, CASE WHEN)
