# 🔧 Build Your Own SLM with PyTorch

- Tokenizer from scratch (BPE, tiktoken, sentencepiece)
- GPT architecture in PyTorch (embeddings, transformer blocks)
- Training loop (DataLoader, gradient accumulation, mixed precision)
- Text generation (greedy, top-k, top-p sampling, temperature)
- Scaling (Flash Attention, gradient checkpointing, ZeRO)
- Reference: Karpathy's nanoGPT
