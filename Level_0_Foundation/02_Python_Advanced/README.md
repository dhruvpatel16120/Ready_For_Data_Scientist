# 🐍 Python Advanced

> OOP, decorators, generators, lambda, context managers, concurrency, type hints, testing

## Topics Covered
- OOP (classes, inheritance, polymorphism, dunder methods)
- Decorators & Closures (@property, @staticmethod, wrappers)
- Generators & Iterators (yield, lazy evaluation)
- Lambda & Functional (map, filter, reduce)
- Context Managers (with statement, contextlib)
- Concurrency (threading, multiprocessing, asyncio)
- Type Hints (typing module, mypy)
- Virtual Environments (venv, conda)
- Testing (unittest, pytest)
