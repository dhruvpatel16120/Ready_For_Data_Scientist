# 📈 Supervised Learning

- Linear Regression (OLS, R², MSE)
- Logistic Regression (sigmoid, log-loss)
- Decision Trees (Gini/entropy, pruning, CART)
- Random Forest (bagging, feature importance)
- Gradient Boosting (XGBoost, LightGBM, CatBoost)
- SVM (kernel trick, margin, C & gamma)
- KNN (distance metrics, K choice)
- Naive Bayes (Bayes theorem, NLP use)
